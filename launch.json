{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "pathMappings": {
                "/var/www/public": "${workspaceRoot}/public"
            },
            "port": 9000,
            "log": true
        },
    ]
}
